# SQLite database file, relative to the project root or an absolute persistent path.
DATABASE_PATH=data/community.sqlite
# Production uses secure session cookies by default. Serve production over HTTPS.
# Set false only for local production testing over plain HTTP.
COOKIE_SECURE=true
